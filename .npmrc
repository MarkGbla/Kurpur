# Windows: reduce nesting and avoid install failures
node-linker=hoisted
# Skip optional deps that can cause EPERM (e.g. fsevents on Windows)
optional=false
# Prefer offline to reduce network timeouts
prefer-offline=true
