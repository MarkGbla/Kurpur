/**
 * Default suggested percentage when the Profile page loads (80%).
 * User can change selection via the 10–90% buttons.
 */
export const SUGGESTED_BUDGET_PERCENT = 80;

/** Options shown in Profile: 10% to 90%. Default suggestion is 80%. */
export const BUDGET_PERCENT_OPTIONS = [
  10, 20, 30, 40, 50, 60, 70, 80, 90,
] as const;
